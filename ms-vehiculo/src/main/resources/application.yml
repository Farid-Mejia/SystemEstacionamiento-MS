spring:
  application:
    name: ms-vehiculo
  datasource:
    url: jdbc:mysql://localhost:3306/db_vehiculo
    username: root
    password: Lifeinmono77
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

server:
  port: 8001

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
  service-url:
    defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true
