server:
  port: 9000

spring:
  application:
    name: ms-users
  cloud:
    compatibility-verifier:
      enabled: false

  # Configuración de base de datos MySQL (Docker)
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/parksystem_usersdb?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: Lifeinmono77
    driver-class-name: com.mysql.cj.jdbc.Driver

  # Configuración JPA
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  # Configuración de Jackson para fechas
  jackson:
    time-zone: America/Lima
    date-format: yyyy-MM-dd HH:mm:ss

# Configuración JWT
jwt:
  secret: ynnZnUmVsXjHBgtOZQuRCiIMeQHPfqXwePYXwiYLIgwsBlvjZe
  expiration: 86400000 # 24 horas en milisegundos

# Configuración de autenticación básica para endpoints públicos
app:
  security:
    basic:
      username: parksystem_api
      password: ParkSystem2024!SecureAPI

# Configuración de logging
logging:
  level:
    com.cibertec: DEBUG
    org.springframework.security: DEBUG

# Configuración de Eureka Client
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true
    hostname: localhost
